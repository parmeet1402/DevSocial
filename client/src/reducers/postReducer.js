import {ADD_POST} from '../..'

const inititalState = {
  post: [],
  posts: [],
  loading: false
};

export default function(state = inititalState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
